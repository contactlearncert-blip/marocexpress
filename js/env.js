/* Généré automatiquement au build Vercel par scripts/generate-env.js à
   partir de la variable d'environnement WEBZA_API_BASE_URL — voir
   vercel.json ("buildCommand"). Ce fichier versionné est un simple
   placeholder de secours pour un lancement en local sans étape de build ;
   il est réécrit à chaque déploiement Vercel. */
window.__WEBZA_ENV__ = {
  API_BASE_URL: "",
};
